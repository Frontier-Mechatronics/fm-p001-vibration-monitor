# Node hardware

The sensing node: accelerometer, acquisition path, MCU and interconnect.

Empty at G0. No sensor or MCU has been selected; both are candidates (see
`docs/decisions/README.md`, "Explicitly NOT decided"). First content is expected at G1 as a
wiring record and `revisions.md` for whatever development hardware the human has on the
bench — the aim of G1 is to learn the measurement method, not to choose a part.

Revision identity: `HW-node-rNN` (see `hardware/README.md`).
