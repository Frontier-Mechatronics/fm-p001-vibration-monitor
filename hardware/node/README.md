# Node hardware

The sensing node: accelerometer, acquisition path, MCU and interconnect.

Empty. **No sensor or MCU has been selected**; both remain candidates (see
`docs/decisions/README.md`, "Explicitly NOT decided").

The G1 experimental hardware — Nucleo F411RE host, EVAL-ADXL355-PMDZ acquisition platform —
is recorded in [`../inventory.md`](../inventory.md). The aim of G1 is to learn the
measurement method, not to choose a part — and whether experimental use has quietly become a
product choice is tested by the R-024 checkpoint before G2 opens
([`../../project/risks.md`](../../project/risks.md)), not asserted here.

First content here is expected once the ADXL355 boards are delivered and wired: a wiring
record and `revisions.md` under `HW-node-rNN`. Nothing is assembled yet.

Revision identity: `HW-node-rNN` (see `hardware/README.md`).
