# Gateway hardware

The Linux edge gateway and its physical setup: host hardware, power, interconnect to the
node, and any site-mounting arrangements.

Empty at G0. Gateway hardware is a candidate, not a decision, and is not needed before G5.

Revision identity: `HW-gateway-rNN` (see `hardware/README.md`).
