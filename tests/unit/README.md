# Unit tests

Host-side tests of cross-cutting logic in isolation — analysis code, tooling, gateway and
cloud modules. Firmware-local unit tests live in `firmware/tests/`.

Empty at G0: no product code exists. See `tests/README.md` for what a unit test may and may
not be used to claim.
