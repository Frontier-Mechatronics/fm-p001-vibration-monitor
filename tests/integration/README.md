# Integration tests

Tests across a component boundary — e.g. gateway ingest against a recorded node output, or
cloud ingest against recorded gateway output. Host-side; no physical hardware.

Integration tests prove that components agree on an interface. They prove nothing about
electrical or timing reality (`tests/README.md`).

Empty at G0.
